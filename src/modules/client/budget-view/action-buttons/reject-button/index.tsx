import IconButton from '@shared/design-system/icon-button';

export default function RejectButton() {
  return (
    <IconButton icon="ThumbDownIcon" color="cancel">
      Rejeitar orçamento
    </IconButton>
  );
}
